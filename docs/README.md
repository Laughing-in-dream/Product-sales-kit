# 知识库（产品需求文档）

这个目录是本项目的**唯一需求真相来源（Source of Truth）**。
选型向导（app.js）里的所有业务规则，都必须能在这里找到对应的文字描述。

## 为什么需要它

代码是由产品设计方案驱动的。比如：

> AD Plus 2.0 搭配 Standard / Plus 电源盒时，最大支持 1 路外接 IPC + 1 路 AHD。

这句话既是"知识"，也是"限制"——它决定了向导里摄像头步骤的数量上限逻辑。
以前这类规则只存在于聊天记录和代码里，改一处忘一处。现在统一沉淀到 `knowledge/` 目录。

## 目录结构

```
docs/
├── README.md            ← 本文件（工作流说明）
└── knowledge/
    ├── _template.md     ← 新建产品文档时复制这个模板
    ├── adplus-2.0.md    ← AD Plus 2.0（规则最完整，自定义向导流程）
    ├── m1n-2.0.md       ← M1N 2.0
    ├── m3n.md           ← M3N
    ├── c6-lite-2.0.md   ← C6 Lite 2.0
    ├── avm.md           ← AVM 360
    ├── z5.md            ← Z5
    ├── 960c53.md        ← 960C53（BSD 单品方案）
    └── 966c46-ipc.md    ← 966C46-IPC（叉车方案）
```

## 工作流（重要）

1. **改代码前，先查知识库。** 任何涉及选型规则的改动，先确认对应产品文档里的规则描述。
2. **新需求先写进知识库，再实现。** 拿到新的产品限制（如"XX 最多支持 N 路"），
   先在对应产品文档的"硬性规则"章节记录，标注来源，然后再改 app.js。
3. **每条规则带三个标签：**
   - `状态`：✅ 已确认（人工/官方资料确认过） / ⚠️ 推断（从现有代码行为反推，待确认）
   - `来源`：谁提供的（产品方案文档 / 钉钉页面 / 口头需求 / 代码反推）
   - `代码位置`：实现这条规则的函数或常量（如 `app.js: cameraCapacityRule()`）
4. **规则变更时，知识库和代码在同一个 commit 里一起改**，commit 信息里说明是哪条规则变了。

## 产品进度看板

按产品逐个推进，每完成一步就把对应格子改成 ✅（可以直接让 AI 改）：

| 产品 | 规则已录入 | 规则已人工确认 | 代码与规则核对一致 |
| --- | --- | --- | --- |
| AD Plus 2.0 | ✅（9 条） | ⬜ 仅规则 1 已确认 | ✅ 规则来自代码反推 |
| M1N 2.0 | ⬜ 仅路数 1 条 | ⬜ | ✅ |
| M3N | ⬜ 仅路数 1 条 | ⬜ | ✅ |
| C6 Lite 2.0 | ⬜ 空 | ⬜ | —（代码中无限制逻辑） |
| AVM 360 | ⬜ 仅 1 条 | ⬜ | ✅ |
| Z5 | ⬜ 空 | ⬜ | — |
| 960C53 | ✅（钉钉页整理） | ✅ | ⬜ 未逐条核对 |
| 966C46-IPC | ⬜ 空 | ⬜ | — |

> 说明："规则来自代码反推"的行，第三列天然一致，但第二列必须人工确认；
> 反过来 960C53 规则可信，但还没核对代码是否全部实现。

## 与其他文件的关系

- `North America Sales List.xlsx` —— 物料清单源数据（SKU、名称、图片），由钉钉页面导出。
- `catalog-data.js` —— 由 `scripts/extract_catalog.py` 从 Excel 生成，**不要手改**。
- `app.js` —— 向导交互 + 业务规则实现。业务规则以本知识库为准。
