# M1N 2.0（产品线 id: `m1n20`）

> M 系列 MDVR 方案，向导走"场景方案 + 摄像头搭配"流程。

## 1. 方案构成

- 向导流程：选场景方案 → 接线 → 摄像头 → 可选件 → 导出
- 步骤对应 Excel 行号见 `app.js: M1N_STEP_ROWS`

## 2. 硬性规则（既是知识也是限制）

| # | 规则 | 状态 | 来源 | 代码位置 |
| --- | --- | --- | --- | --- |
| 1 | 最大支持 **2 路 IPC + 4 路 AHD** | ⚠️ 推断自代码 | 代码反推 | `app.js: M_SERIES_CHANNEL_RULES` |

## 3. 选配关系

- 摄像头延长线行号：AHD → rows 24-27，IPC → rows 28-30（`M3N_CAMERA_EXTENSION_ROWS`，M 系列共用）。

## 4. 关键 SKU

见 `catalog-data.js`（M1N 2_0 产品线）。

## 5. 待确认问题

- [ ] 2 路 IPC + 4 路 AHD 是否为同时上限（还是总路数另有限制）？
- [ ] 各场景方案（scenario）的单选/多选规则尚未结构化。
